#include<stdio.h>
int main()
{
    double  mealCost;
    int tipPercent, taxPercent, total;
    double tip, tax, totalCost;
    scanf("%d",  &tipPercent);
    //tip=(double)(tipPercent/100);
    printf("%lf",tip);
    return 0;
}

